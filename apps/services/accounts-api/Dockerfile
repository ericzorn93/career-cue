# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build accounts-api`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t accounts-api`.
FROM golang:1.23-alpine AS builder
RUN apk update --no-cache bash curl
WORKDIR /app
COPY ../../. .
ENV GOOS=linux
ENV GOARCH=amd64
ENV CGO_ENABLED=0
RUN go build -o /bin/accounts-api ./apps/services/accounts-api/cmd/server/main.go

FROM scratch
WORKDIR /app
COPY --from=builder /bin/accounts-api .
CMD [ "./accounts-api" ]